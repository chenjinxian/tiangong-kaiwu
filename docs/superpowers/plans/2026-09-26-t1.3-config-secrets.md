# T1.3 配置与密钥整治 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** modeling-server 与 webhook-agent 达到内网交付底线：zod 校验配置、密钥无默认必填、缺配置拒启并指名缺失项、服务间认证强制、env 命名统一、首启生成脚本。

**Architecture:** 每服务一个 `src/config.ts`（zod schema，启动即 parse，导出 frozen config，失败打印缺失清单 exit 1）；全部 `process.env` 读取收敛到该模块；仓库根单份 `.env` 由 `scripts/generate-env.ps1` 幂等生成（Node 原生 `process.loadEnvFile` 加载，零新运行时依赖，仅 zod）。

**Tech Stack:** TypeScript、zod（新增 devDep→dep）、tsx、Vitest、PowerShell。

**Spec:** `docs/superpowers/specs/2026-09-26-t1.3-config-secrets-design.md`

## Global Constraints

- 密钥项一律必填无默认：`BACKEND_API_KEY`、`WEBAGENT_API_KEY`、`CSRF_SECRET`、`IMODELHUB_ADMIN_PASSWORD`（MS）；`AZURITE_ACCOUNT_KEY`、`WEBHOOK_SECRET`、`IMODELHUB_API_KEY`、`IMODELHUB_ADMIN_EMAIL`、`IMODELHUB_ADMIN_PASSWORD`（WA）。缺失→启动退出码 1 且输出含变量名。
- 非密钥项（URL/端口/路径/开关）保留 dev 默认值（spec schema 表为准）。
- 命名统一破坏式：弃用 `BLOB_*`、`AZURITE_URL`、`IMODELHUB_API_URL`、`BACKEND_URL`、`IMODELHUB_API_KEY` 仅在 WA 侧统一拼写；统一为 `AZURITE_*`、`MODELING_SERVER_URL`、`IMODELHUB_URL`。
- 仓库根单份 `.env`；`.env.example` 入库（需 `.gitignore` 加 `!.env.example`，现有规则 `.env.*` 会吞掉它）。
- 不动：WS token 鉴权（T1.4）、HUB 代码（T2.4）、compose（T1.4）、前端。
- 已知存量失败不修不掩盖：`modeling-server/src/projects/service.test.ts`（mock 构造器问题，T2.2）。
- 提交信息结尾加 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。

---

### Task 1: modeling-server 配置模块

**Files:**
- Create: `modeling-server/src/config.ts`
- Test: `modeling-server/src/config.test.ts`
- Modify: `modeling-server/package.json`（zod 依赖）

**Interfaces:**
- Consumes: 无（首个任务）。
- Produces: `export const config: Config`（frozen），字段名即 schema 表：`PORT: number`、`IMODELHUB_URL`、`WEBAGENT_URL`、`FRONTEND_URL`、`ALLOWED_ORIGINS`、`AZURITE_ACCOUNT_NAME`、`AZURITE_HOST`、`BRIEFCASE_CACHE_LOCATION`、`IMODELHUB_ADMIN_EMAIL`、`IMODELHUB_ADMIN_PASSWORD`、`BACKEND_API_KEY`、`WEBAGENT_API_KEY`、`CSRF_SECRET`、`LOG_LEVEL`、`NODE_ENV`。Task 2/5 依赖这些确切字段名。

- [ ] **Step 1: 安装 zod**

```bash
cd modeling-server && corepack pnpm@10 add zod
```

- [ ] **Step 2: 写失败测试 `src/config.test.ts`**

```typescript
/**
 * Copyright (c) LubanCAD. All rights reserved.
 * Licensed under the MIT License.
 *
 * Config module: secrets are mandatory (missing => exit 1 naming the var),
 * non-secrets carry dev defaults.
 */
import { describe, expect, it } from 'vitest';
import { execFileSync } from 'node:child_process';
import * as path from 'node:path';

const projectDir = path.resolve(__dirname, '..');

function runConfigImport(env: Record<string, string>): { status: number; output: string } {
  try {
    const output = execFileSync(
      process.execPath,
      ['--import', 'tsx', '-e', "import('./src/config.ts').then(() => console.log('CONFIG_OK'))"],
      { cwd: projectDir, env: { ...process.env, ...env }, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] }
    );
    return { status: 0, output };
  } catch (e) {
    const err = e as { status?: number; stdout?: string; stderr?: string };
    return { status: err.status ?? -1, output: `${err.stdout ?? ''}${err.stderr ?? ''}` };
  }
}

const SECRET_VARS = [
  'BACKEND_API_KEY', 'WEBAGENT_API_KEY', 'CSRF_SECRET',
  'IMODELHUB_ADMIN_EMAIL', 'IMODELHUB_ADMIN_PASSWORD',
] as const;

const allSecrets: Record<string, string> = {
  BACKEND_API_KEY: 'a'.repeat(32),
  WEBAGENT_API_KEY: 'b'.repeat(32),
  CSRF_SECRET: 'c'.repeat(32),
  IMODELHUB_ADMIN_EMAIL: 'admin@test.local',
  IMODELHUB_ADMIN_PASSWORD: 'd'.repeat(16),
};

describe('modeling-server config', () => {
  it.each(SECRET_VARS)('exits 1 naming %s when it is missing', (varName) => {
    const env = { ...allSecrets };
    delete env[varName];
    const result = runConfigImport(env);
    expect(result.status).toBe(1);
    expect(result.output).toContain(varName);
  });

  it('exports typed config with dev defaults when all secrets are present', () => {
    const result = runConfigImport(allSecrets);
    expect(result.status).toBe(0);
    expect(result.output).toContain('CONFIG_OK');
  });
});
```

注意：测试用真实环境变量覆盖；若本机根 `.env` 已存在，`process.loadEnvFile` 会补齐缺失项导致「缺失拒启」用例失败——测试运行前须确保无根 `.env`（CI 天然如此；本地若已有，跑测试时临时改名）。

- [ ] **Step 3: 跑测试确认失败**

Run: `cd modeling-server && npx vitest run src/config.test.ts`
Expected: FAIL（模块不存在 / 导入报错）

- [ ] **Step 4: 实现 `src/config.ts`**

```typescript
/**
 * Copyright (c) LubanCAD. All rights reserved.
 * Licensed under the MIT License.
 *
 * Single source of configuration. Loads the repo-root .env (via Node's
 * native process.loadEnvFile; real environment variables take precedence),
 * validates everything with zod, and exits naming every invalid/missing
 * variable on failure. Secrets have NO defaults — deliberate.
 */
import * as fs from 'node:fs';
import * as path from 'node:path';
import { z } from 'zod';

function loadRootEnvFile(): void {
  // Walk up from this file until a directory containing .env is found
  // (works from both src/ during dev and dist/ after build).
  let dir = __dirname;
  for (let i = 0; i < 6; i++) {
    const candidate = path.join(dir, '.env');
    if (fs.existsSync(candidate)) {
      try { process.loadEnvFile(candidate); } catch { /* malformed file: schema will report */ }
      return;
    }
    dir = path.dirname(dir);
  }
}
loadRootEnvFile();

const url = (defaultValue: string) => z.string().url().default(defaultValue);

const schema = z.object({
  PORT: z.coerce.number().int().positive().default(4001),
  IMODELHUB_URL: url('http://localhost:4000'),
  WEBAGENT_URL: url('http://localhost:4002'),
  FRONTEND_URL: url('http://localhost:3000'),
  ALLOWED_ORIGINS: z.string().optional(),
  AZURITE_ACCOUNT_NAME: z.string().min(1).default('devstoreaccount1'),
  AZURITE_HOST: z.string().min(1).default('127.0.0.1:10000'),
  BRIEFCASE_CACHE_LOCATION: z.string().min(1).default('./briefcase-cache'),

  // --- secrets: mandatory, no defaults ---
  IMODELHUB_ADMIN_EMAIL: z.string().email(),
  IMODELHUB_ADMIN_PASSWORD: z.string().min(12),
  BACKEND_API_KEY: z.string().min(32),
  WEBAGENT_API_KEY: z.string().min(32),
  CSRF_SECRET: z.string().min(32),

  LOG_LEVEL: z.enum(['error', 'warn', 'info', 'debug']).default('info'),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
});

export type Config = z.infer<typeof schema>;

function load(): Config {
  const parsed = schema.safeParse(process.env);
  if (!parsed.success) {
    console.error('modeling-server: invalid configuration —');
    for (const issue of parsed.error.issues) {
      console.error(`  ${issue.path.join('.')}: ${issue.message}`);
    }
    console.error('Provide the missing values in the repo-root .env (see .env.example).');
    console.error('First run: powershell -File scripts/generate-env.ps1');
    process.exit(1);
  }
  return Object.freeze(parsed.data);
}

export const config = load();
```

（TS 版本低于 5.x 的 `process.loadEnvFile` 类型可用 `declare` 或 `@types/node` 已含；若类型报错，用 `(process as any).loadEnvFile?.(candidate)`。）

- [ ] **Step 5: 跑测试确认通过**

Run: `cd modeling-server && npx vitest run src/config.test.ts`
Expected: PASS（5 缺失用例 + 1 成功用例）

- [ ] **Step 6: 提交**

```bash
git add modeling-server/src/config.ts modeling-server/src/config.test.ts modeling-server/package.json modeling-server/pnpm-lock.yaml
git commit -m "feat(modeling-server): zod config module — secrets mandatory, fail-fast startup"
```

---

### Task 2: modeling-server env 收敛 + 入站强制认证

**Files:**
- Modify: `modeling-server/src/main.ts`（删除 34-44 行 env 常量块，全部改 `config.*`；309 行与 280-288 行认证改强制）、`modeling-server/src/middleware/csrf.ts`、`modeling-server/src/middleware/security.ts`、`modeling-server/src/rpc/OpenCloudRpcImpl.ts`（仅 env 引用点）、`modeling-server/src/ipc/*.ts`（仅 env 引用点）
- Test: `modeling-server/src/middleware/webhookAuth.test.ts`（新建）

**Interfaces:**
- Consumes: Task 1 的 `config`（字段名见 Task 1 Produces）。
- Produces: `main.ts` 中三处入站路由（`/api/webhook/events`、`POST /api/imodels/:id/progress`）无条件校验 `X-API-Key === config.BACKEND_API_KEY`（不符 401）；`/api/imodels/:id/retry` 出站恒带 `X-API-Key: config.WEBAGENT_API_KEY`。Task 5 依赖此行为。

- [ ] **Step 1: 写失败测试 `src/middleware/webhookAuth.test.ts`**

```typescript
/**
 * Copyright (c) LubanCAD. All rights reserved.
 * Licensed under the MIT License.
 *
 * Inbound webhook-agent routes REQUIRE the API key — no optional mode.
 */
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import express from 'express';
import type { Server } from 'node:http';
import { AddressInfo } from 'node:net';

process.env.BACKEND_API_KEY = 'k'.repeat(32);
process.env.CSRF_SECRET = 'c'.repeat(32);
process.env.IMODELHUB_ADMIN_EMAIL = 'admin@test.local';
process.env.IMODELHUB_ADMIN_PASSWORD = 'p'.repeat(16);
process.env.WEBAGENT_API_KEY = 'g'.repeat(32);

const realFetch = global.fetch;
let server: Server;
let baseUrl: string;

beforeEach(async () => {
  // main.ts cannot be imported wholesale (starts IModelHost); register the
  // guarded routes via the same helper extracted in Step 3.
  const { createWebhookEventRoutes } = await import('./webhookEvents.js');
  const app = express();
  app.use(express.json());
  app.use(createWebhookEventRoutes());
  server = app.listen(0);
  baseUrl = `http://127.0.0.1:${(server.address() as AddressInfo).port}`;
});

afterEach(async () => {
  vi.unstubAllGlobals();
  await new Promise<void>((resolve) => server.close(() => resolve()));
});

describe('webhook-agent inbound auth', () => {
  it('rejects /api/webhook/events without a key with 401', async () => {
    const res = await fetch(`${baseUrl}/api/webhook/events`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' }, body: '{}',
    });
    expect(res.status).toBe(401);
  });

  it('rejects /api/webhook/events with a wrong key with 401', async () => {
    const res = await fetch(`${baseUrl}/api/webhook/events`, {
      method: 'POST', headers: { 'Content-Type': 'application/json', 'X-API-Key': 'wrong' }, body: '{}',
    });
    expect(res.status).toBe(401);
  });

  it('accepts /api/webhook/events with the configured key', async () => {
    const res = await fetch(`${baseUrl}/api/webhook/events`, {
      method: 'POST', headers: { 'Content-Type': 'application/json', 'X-API-Key': 'k'.repeat(32) },
      body: JSON.stringify({ eventType: 'iModels.iModelCreated.v1', iModelId: 'x', id: 'e1' }),
    });
    expect(res.status).toBe(200);
  });

  it('rejects progress POST without a key with 401', async () => {
    const res = await fetch(`${baseUrl}/api/imodels/x/progress`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' }, body: '{"step":"s"}',
    });
    expect(res.status).toBe(401);
  });
});

void realFetch;
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd modeling-server && npx vitest run src/middleware/webhookAuth.test.ts`
Expected: FAIL（`./webhookEvents.js` 不存在）

- [ ] **Step 3: 实现**

新建 `modeling-server/src/middleware/webhookEvents.ts`，把 main.ts 中两条 webhook-agent 入站路由原样迁入并加强制校验（路由逻辑不变，仅认证变）：

```typescript
/**
 * Copyright (c) LubanCAD. All rights reserved.
 * Licensed under the MIT License.
 *
 * Inbound routes from webhook-agent. The API key is REQUIRED — there is no
 * optional mode (config enforces its presence at startup).
 */
import { Router, type Request, type Response } from 'express';
import { config } from '../config.js';
import { logger } from '../utils/logger.js';

export function createWebhookEventRoutes(): Router {
  const router = Router();

  const requireApiKey = (req: Request, res: Response): boolean => {
    if (req.headers['x-api-key'] !== config.BACKEND_API_KEY) {
      res.status(401).json({ error: 'Unauthorized' });
      return false;
    }
    return true;
  };

  router.post('/api/webhook/events', (req: Request, res: Response) => {
    if (!requireApiKey(req, res)) return;
    const event = req.body;
    logger.info(`[Webhook] Received event: ${event.eventType} for iModel ${event.iModelId}`);
    res.json({ received: true, eventId: event.id });
  });

  router.post('/api/imodels/:id/progress', (req: Request, res: Response) => {
    if (!requireApiKey(req, res)) return;
    const { step, progress } = req.body as { step?: string; progress?: number };
    if (typeof step !== 'string' || (progress !== undefined && typeof progress !== 'number')) {
      res.status(400).json({ error: 'Invalid progress payload' });
      return;
    }
    // progress store stays in main.ts; emit via callback registered by main
    onProgress?.(req.params.id, step, progress ?? 0);
    res.json({ received: true });
  });

  return router;
}

// main.ts registers its in-memory progress map here (avoids duplicating state).
let onProgress: ((id: string, step: string, progress: number) => void) | undefined;
export function registerProgressHandler(fn: (id: string, step: string, progress: number) => void): void {
  onProgress = fn;
}
```

然后改 `main.ts`：
1. 顶部 `import { config } from './config.js'; import { createWebhookEventRoutes, registerProgressHandler } from './middleware/webhookEvents.js';`
2. 删除 34-44 行的 env 常量块；文件内所有 `PORT/IMODELHUB_URL/...` 引用改为 `config.PORT` 等（`process.env.IMJS_AZURE_BLOB_BASE_URI` 赋值改为用 `config.AZURITE_HOST/AZURITE_ACCOUNT_NAME` 计算后仍写入该 process.env 键——CheckpointManager 读它，属 itwinjs-core 契约不可改）。
3. 删除 main.ts 内两条内联路由（webhook-events、progress POST），换 `app.use(createWebhookEventRoutes()); registerProgressHandler((id, step, progress) => { /* 原 iModelProgressMap 逻辑 */ });`（进度 GET 路由保留原地，仅 env 引用改 config）。
4. `/api/imodels/:id/retry` 出站 fetch 恒带 `'X-API-Key': config.WEBAGENT_API_KEY`（删除 if 包裹）。
5. `grep -rn "process\.env\." src/ --include=*.ts | grep -v config.ts | grep -v test` 应只剩 `IMJS_AZURE_BLOB_BASE_URI`（写入）与 NODE_ENV 判断（如无必要也换 config.NODE_ENV）；`csrf.ts`/`security.ts`/`OpenCloudRpcImpl.ts`/`ipc/*.ts` 中的 env 读取同样改 config（CSRF_SECRET、ALLOWED_ORIGINS、IMODELHUB_URL、BRIEFCASE_CACHE_LOCATION、IMODELHUB_ADMIN_*）。

- [ ] **Step 4: 跑新旧测试 + tsc**

Run: `cd modeling-server && npx vitest run src/middleware/webhookAuth.test.ts src/middleware src/routes && npx tsc --noEmit`
Expected: 全 PASS / tsc 仅存量 `projects/service.test.ts` 报错

- [ ] **Step 5: 提交**

```bash
git add modeling-server/src
git commit -m "feat(modeling-server): converge env reads to config; webhook-agent auth mandatory"
```

---

### Task 3: webhook-agent 配置模块 + 命名统一 + 出站恒带 key

**Files:**
- Create: `webhook-agent/src/config.ts`、`webhook-agent/src/config.test.ts`
- Modify: `webhook-agent/src/main.ts`、`webhook-agent/src/baseline-generator.ts`、`webhook-agent/src/forwarder.ts`（env 引用点）
- Modify: `webhook-agent/package.json`（zod）

**Interfaces:**
- Consumes: 无跨任务依赖（与 Task 1 同构）。
- Produces: WA 的 `config` 字段：`PORT`、`IMODELHUB_URL`、`MODELING_SERVER_URL`、`AZURITE_ACCOUNT_NAME`、`AZURITE_HOST`、`AZURITE_ACCOUNT_KEY`、`WEBHOOK_SECRET`、`IMODELHUB_API_KEY`、`IMODELHUB_ADMIN_EMAIL`、`IMODELHUB_ADMIN_PASSWORD`、`LOG_LEVEL`、`RECOVERY_*`、`DISABLE_AUTOMATIC_RECOVERY`。Task 4/5 依赖。

- [ ] **Step 1: 安装 zod**

```bash
cd webhook-agent && corepack pnpm@10 add zod
```

- [ ] **Step 2: 写失败测试 `src/config.test.ts`**（与 Task 1 Step 2 同构，改变量集）

必填集：`AZURITE_ACCOUNT_KEY`、`WEBHOOK_SECRET`、`IMODELHUB_API_KEY`、`IMODELHUB_ADMIN_EMAIL`、`IMODELHUB_ADMIN_PASSWORD`。缺失任意一个 → 子进程退出码 1 且输出含变量名；齐全 → 导出成功打印 CONFIG_OK。默认值断言不必子进程化——直接 `import { config } from './config.js'`（子进程已注入全量 env）断言 `config.PORT === 4002`、`config.IMODELHUB_URL === 'http://localhost:4000'`、`config.MODELING_SERVER_URL === 'http://localhost:4001'`。

- [ ] **Step 3: 跑测试确认失败**

Run: `cd webhook-agent && npx vitest run src/config.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 4: 实现 `src/config.ts`**（结构与 Task 1 Step 4 完全同构，schema 换为）

```typescript
const schema = z.object({
  PORT: z.coerce.number().int().positive().default(4002),
  IMODELHUB_URL: url('http://localhost:4000'),
  MODELING_SERVER_URL: url('http://localhost:4001'),   // replaces BACKEND_URL
  AZURITE_ACCOUNT_NAME: z.string().min(1).default('devstoreaccount1'),
  AZURITE_HOST: z.string().min(1).default('127.0.0.1:10000'),

  // --- secrets: mandatory ---
  AZURITE_ACCOUNT_KEY: z.string().min(16),             // replaces BLOB_ACCOUNT_KEY + hardcoded well-known key
  WEBHOOK_SECRET: z.string().min(32),
  IMODELHUB_API_KEY: z.string().min(32),
  IMODELHUB_ADMIN_EMAIL: z.string().email(),
  IMODELHUB_ADMIN_PASSWORD: z.string().min(12),

  LOG_LEVEL: z.enum(['error', 'warn', 'info', 'debug']).default('info'),
  RECOVERY_CHECK_INTERVAL_MINUTES: z.coerce.number().int().positive().default(5),
  RECOVERY_MAX_PER_CHECK: z.coerce.number().int().positive().default(10),
  DISABLE_AUTOMATIC_RECOVERY: z.coerce.boolean().default(false),
});
```

同时全量替换（`grep -rn "process\.env\." src/ --include=*.ts | grep -v config.ts`）：
- `BACKEND_URL` → `config.MODELING_SERVER_URL`
- `BLOB_ACCOUNT_NAME/BLOB_ACCOUNT_KEY/BLOB_STORAGE_URL/BLOB_CONTAINER_NAME` → `config.AZURITE_*`（`BLOB_STORAGE_URL` 的默认 `http://127.0.0.1:10000/devstoreaccount1` 改由 `AZURITE_HOST/ACCOUNT_NAME` 组装；`BLOB_CONTAINER_NAME` 若有独立值并入使用点常量或 config 可选项，以 grep 实际使用为准）
- `IMODELHUB_API_URL` → `config.IMODELHUB_URL`
- WA→HUB 的**全部**出站 fetch（baseline-generator 的 upload-url/confirm/poll/failed、main.ts 的 admin/uninitialized）headers 恒带 `'X-API-Key': config.IMODELHUB_API_KEY`（删除「设了才带」的 if）
- `webhook-agent/src/main.ts:79` 的 Azurite well-known key 默认值删除

- [ ] **Step 5: 跑测试确认通过 + tsc**

Run: `cd webhook-agent && npx vitest run && npx tsc --noEmit`
Expected: config 测试 PASS；tsc 0 错误

- [ ] **Step 6: 提交**

```bash
git add webhook-agent/src webhook-agent/package.json webhook-agent/pnpm-lock.yaml
git commit -m "feat(webhook-agent): zod config, unified AZURITE_*/MODELING_SERVER_URL naming, mandatory hub API key"
```

---

### Task 4: 首启生成脚本 + .env.example + 文档

**Files:**
- Create: `scripts/generate-env.ps1`、`.env.example`
- Modify: `.gitignore`（加 `!.env.example`）、`CLAUDE.md`（常用命令节）

**Interfaces:**
- Consumes: Task 1/3 的 schema 变量全集。
- Produces: 幂等生成根 `.env`；`.env.example` 与两服务 schema 一一对应；Task 5 依赖脚本可用。

- [ ] **Step 1: 写 `.env.example`**（内容完整如下）

```bash
# tiangong-kaiwu 本地/内网部署环境变量（唯一事实源）
# 首次使用：powershell -File scripts/generate-env.ps1  生成本 .env 的随机密钥版
# 规则：密钥项必填无默认；URL/端口项有 dev 默认，内网部署按需覆盖。

# ---------- 通用 ----------
# IMODELHUB_ADMIN_EMAIL=admin@example.com        # HUB 管理账号（需与 imodelhub-services 侧一致）
# IMODELHUB_ADMIN_PASSWORD=                      # >=12 位，generate-env 生成

# ---------- modeling-server (4001) ----------
# BACKEND_API_KEY=                               # 32+ 位随机，验 webhook-agent 入站
# WEBAGENT_API_KEY=                              # 32+ 位随机，调 webhook-agent 出站
# CSRF_SECRET=                                   # 32+ 位随机
# PORT=4001
# IMODELHUB_URL=http://localhost:4000
# WEBAGENT_URL=http://localhost:4002
# FRONTEND_URL=http://localhost:3000
# AZURITE_ACCOUNT_NAME=devstoreaccount1
# AZURITE_HOST=127.0.0.1:10000
# BRIEFCASE_CACHE_LOCATION=./briefcase-cache

# ---------- webhook-agent (4002) ----------
# AZURITE_ACCOUNT_KEY=                           # Azurite 账号密钥（本地 Azurite 为 well-known key，内网真实存储换真值）
# WEBHOOK_SECRET=                                # 32+ 位随机，HMAC 验签（需与 imodelhub-services webhook 订阅一致）
# IMODELHUB_API_KEY=                             # 32+ 位随机，WA→HUB 出站
# MODELING_SERVER_URL=http://localhost:4001

# ---------- 可选 ----------
# LOG_LEVEL=info
# RECOVERY_CHECK_INTERVAL_MINUTES=5
# RECOVERY_MAX_PER_CHECK=10
# DISABLE_AUTOMATIC_RECOVERY=false
```

- [ ] **Step 2: 写 `scripts/generate-env.ps1`**

```powershell
# ============================================================
# generate-env.ps1 — 幂等生成仓库根 .env（随机密钥版 .env.example）
# 用法: powershell -File scripts/generate-env.ps1 [-Force]
# -Force: 覆盖已存在的 .env（默认跳过）
# ============================================================
param([switch]$Force)
$ErrorActionPreference = 'Stop'
Set-Location (Split-Path -Parent $PSScriptRoot)

if ((Test-Path .env) -and -not $Force) {
  Write-Host ".env 已存在，跳过生成（覆盖请加 -Force）" -ForegroundColor Yellow
  exit 0
}

function New-Hex([int]$Bytes) {
  $b = [byte[]]::new($Bytes); [System.Security.Cryptography.RandomNumberGenerator]::Fill($b)
  ($b | ForEach-Object { $_.ToString('x2') }) -join ''
}

$envMap = [ordered]@{
  'IMODELHUB_ADMIN_EMAIL'    = 'admin@example.com'
  'IMODELHUB_ADMIN_PASSWORD' = New-Hex 16
  'BACKEND_API_KEY'          = New-Hex 32
  'WEBAGENT_API_KEY'         = New-Hex 32
  'CSRF_SECRET'              = New-Hex 32
  'AZURITE_ACCOUNT_KEY'      = 'Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw=='  # Azurite well-known（真实存储时换）
  'WEBHOOK_SECRET'           = New-Hex 32
  'IMODELHUB_API_KEY'        = New-Hex 32
}

$lines = @('# 由 scripts/generate-env.ps1 生成于 ' + (Get-Date -Format 'yyyy-MM-dd HH:mm:ss'), '# 完整变量说明见 .env.example', '')
foreach ($k in $envMap.Keys) { $lines += "$k=$($envMap[$k])" }

Set-Content -Path .env -Value ($lines -join "`r`n") -Encoding utf8
Write-Host ".env 已生成（10 项）。注意：" -ForegroundColor Green
Write-Host "  1) WEBHOOK_SECRET 需与 imodelhub-services 的 webhook 订阅 secret 一致（见 docs/WEBHOOK_CONFIG.md）"
Write-Host "  2) IMODELHUB_ADMIN_EMAIL/PASSWORD 需与 HUB 侧账号对齐（HUB 自带 seed，不一致时改 HUB 侧或本文件）"
```

- [ ] **Step 3: `.gitignore` 加例外 + CLAUDE.md 补一行**

`.gitignore` 在 `.env.*` 行后加：
```
!.env.example
```

`CLAUDE.md`「### 完整本地栈启动顺序」节的第一步前插入：
```markdown
0. 首次运行先生成密钥配置：`powershell -File scripts/generate-env.ps1`（幂等；两后端服务的 `.env` 均读仓库根这一份，密钥缺失会拒启）
```

- [ ] **Step 4: 验证脚本幂等 + 生成结果**

```powershell
powershell -File scripts/generate-env.ps1     # 生成
powershell -File scripts/generate-env.ps1     # 第二次应提示跳过
Select-String -Path .env -Pattern 'BACKEND_API_KEY=' | ForEach-Object { $_.Line.Length -gt 40 }
```
Expected: 第一次「已生成」，第二次「跳过」，key 行长度 > 40。

- [ ] **Step 5: 提交**

```bash
git add scripts/generate-env.ps1 .env.example .gitignore CLAUDE.md
git commit -m "feat: first-run env generation script + .env.example template"
```

---

### Task 5: 全栈验证与收尾

**Files:**
- 无新文件（验证 + 可能的小修）

**Interfaces:**
- Consumes: Task 1-4 全部产物。
- Produces: T1.3 验收证据。

- [ ] **Step 1: 两服务全量测试**

```bash
cd modeling-server && npx vitest run   # 除 projects/service.test.ts（存量）外全绿
cd ../webhook-agent && npx vitest run  # 全绿
```

- [ ] **Step 2: 拒启冒烟（真实行为）**

```powershell
# 临时移走 .env，启动应拒启并指名缺失变量
Rename-Item .env env.bak
cd modeling-server; $p = Start-Process node -ArgumentList '--import','tsx','src/main.ts' -NoNewWindow -PassThru -RedirectStandardError err.txt; Start-Sleep 5; Stop-Process $p -Force; Get-Content err.txt | Select-String 'BACKEND_API_KEY'
cd ..; Rename-Item env.bak .env
```
Expected: err.txt 含 `BACKEND_API_KEY` 与「invalid configuration」。

- [ ] **Step 3: 带配置启动冒烟**

```powershell
cd modeling-server; corepack pnpm@10 dev   # 后台起，确认 'LubanCAD Backend running' 出现且无 config 报错；停掉
cd ../webhook-agent; corepack pnpm@10 dev  # 同上确认启动；停掉
```

- [ ] **Step 4: 残留检查**

```bash
grep -rn "process\.env\." modeling-server/src webhook-agent/src --include=*.ts | grep -v "config.ts" | grep -v "\.test\.ts" | grep -v "IMJS_AZURE_BLOB_BASE_URI" | grep -v "process\.env\.NODE_ENV"
```
Expected: 空输出（IMJS_AZURE_BLOB_BASE_URI 为 itwinjs-core 契约写入点，保留；NODE_ENV 若仍有读取点改 config.NODE_ENV 后也应为空）。

- [ ] **Step 5: 提交（如有小修）+ 汇报**

无新提交则直接汇报验收结果。
