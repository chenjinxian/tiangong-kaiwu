# T2.1 webhook-agent 整治 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** webhook-agent 达到内网交付底线：结构化日志、核心路径测试、空 catch 治理、IModelHost 生命周期配对、去重状态持久化。

**Architecture:** 镜像 modeling-server 的零依赖 JSON logger；测试先行覆盖 validator/forwarder/processor/webhook-server/baseline 纯函数；生命周期收敛为全局一次启停 + 信号处理；去重 Set 换 JSON 文件存储（load/add/persist/TTL）。

**Tech Stack:** TypeScript、Vitest（已装）、Node 原生 fs/crypto，零新依赖。

**Spec:** `docs/superpowers/specs/2026-09-26-t2.1-webhook-agent-hardening-design.md`

## Global Constraints

- 零新运行时依赖（logger 镜像 `modeling-server/src/utils/logger.ts`，去重 JSON 文件，不引 pino/SQLite）。
- 业务文件 `grep -c "console\."` 归零（`src/config.ts` 拒启 stderr 输出与 `*.test.ts` 除外）。
- 不改 WA 对外契约（HUB/MS 接口已在 T1.2/T1.3 定型）。
- 行为变更必须先有失败测试；纯机械替换（Task 2）以既有套件保持绿为验证。
- 已知存量：无（WA 当前 12 用例全绿、tsc 干净）。
- 提交信息结尾 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。

---

### Task 1: logger 模块（镜像 + 单测）

**Files:**
- Create: `webhook-agent/src/utils/logger.ts`
- Test: `webhook-agent/src/utils/logger.test.ts`

**Interfaces:**
- Consumes: `config.LOG_LEVEL`（`src/config.ts` 既有字段）。
- Produces: `export enum LogLevel`、`export const logger: { trace/debug/info/warn/error/fatal(message: string, context?: object): void; setLevel(l: LogLevel): void }`。Task 2/3/4/5/6/7 全部依赖此接口。

- [ ] **Step 1: 写失败测试 `src/utils/logger.test.ts`**

```typescript
import { afterEach, describe, expect, it, vi } from 'vitest';
import { LogLevel, logger } from './logger.js';

describe('logger', () => {
  afterEach(() => logger.setLevel(LogLevel.INFO));

  it('emits one JSON line per call with level, message, context, timestamp, service', () => {
    const lines: string[] = [];
    const spy = vi.spyOn(console, 'log').mockImplementation((l: string) => lines.push(l));
    logger.info('baseline done', { iModelId: 'im-1' });
    spy.mockRestore();
    expect(lines).toHaveLength(1);
    const entry = JSON.parse(lines[0]);
    expect(entry).toMatchObject({ level: 'info', message: 'baseline done', service: 'webhook-agent', iModelId: 'im-1' });
    expect(typeof entry.timestamp).toBe('string');
  });

  it('suppresses levels below the configured threshold', () => {
    const lines: string[] = [];
    const spy = vi.spyOn(console, 'log').mockImplementation((l: string) => lines.push(l));
    logger.setLevel(LogLevel.WARN);
    logger.info('should not appear');
    logger.warn('should appear');
    spy.mockRestore();
    expect(lines).toHaveLength(1);
    expect(JSON.parse(lines[0]).level).toBe('warn');
  });

  it('writes errors to console.error with level error', () => {
    const err = vi.spyOn(console, 'error').mockImplementation(() => {});
    logger.error('boom', { code: 500 });
    expect(err).toHaveBeenCalledTimes(1);
    err.mockRestore();
  });
});
```

- [ ] **Step 2: RED** — Run: `cd webhook-agent && npx vitest run src/utils/logger.test.ts`，Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现** — 通读 `modeling-server/src/utils/logger.ts`（零依赖 JSON 行式 logger），复制到 `webhook-agent/src/utils/logger.ts` 并做且仅做以下适配：服务名常量 `'webhook-agent'`；版本读取自 `../../package.json`；初始 level 取 `config.LOG_LEVEL`（`import { config } from '../config.js'`）；其余（LogLevel 枚举、LogEntry 形状、console.log/error 分流、级别过滤）逐字保留。

- [ ] **Step 4: GREEN** — Run: `npx vitest run src/utils/logger.test.ts`，Expected: 3/3 PASS

- [ ] **Step 5: Commit** — `git add webhook-agent/src/utils` → `feat(webhook-agent): structured logger mirroring modeling-server`

---

### Task 2: 日志全量替换 + 死脚本删除

**Files:**
- Modify: `webhook-agent/src/{baseline-generator,main,webhook-server,forwarder,processor}.ts`
- Delete: `webhook-agent/src/test-baseline.ts`（已核零引用）

**Interfaces:**
- Consumes: Task 1 的 `logger`。
- Produces: 业务文件 console 归零（验收 grep 命令见 Step 3）。

- [ ] **Step 1: 机械替换** — 五文件中每处 `console.log` → `logger.info`/`logger.debug`（一次性流程日志用 info，高频/细节用 debug）；`console.error` → `logger.error`；`console.warn` → `logger.warn`。删除每处伴随的 `// eslint-disable-next-line no-console` 行。文件头加 `import { logger } from './utils/logger.js';`。删除 `src/test-baseline.ts`。

- [ ] **Step 2: 套件回归** — Run: `npx vitest run && npx tsc --noEmit`，Expected: 15/15 PASS（12 既有 + Task 1 的 3）、tsc 0 错

- [ ] **Step 3: 验收 grep** — `grep -rn "console\." src --include=*.ts | grep -v "\.test\.ts" | grep -v "config.ts"` → 仅 logger.ts 自身（其对 console 的使用是唯一出口）。不匹配则补齐遗漏。

- [ ] **Step 4: Commit** — `git add webhook-agent/src` → `refactor(webhook-agent): replace 123 console.* with structured logger; drop dead test-baseline script`

---

### Task 3: validator 测试 + 吞错修复

**Files:**
- Test: `webhook-agent/src/validator.test.ts`
- Modify: `webhook-agent/src/validator.ts`（104 行空 catch）

**Interfaces:**
- Consumes: `src/config.ts` 的 `WEBHOOK_SECRET`。
- Produces: `validateRequest` 行为锁定（有效签名放行；无效 401；body 篡改拒绝；**解析失败不再吞成「无效签名」而是显式错误**——若现实现已正确区分则仅加测试）。

- [ ] **Step 1: 写失败/特征测试**

```typescript
import { createHmac, timingSafeEqual } from 'node:crypto';
import { describe, expect, it, beforeEach } from 'vitest';
import { validateRequest } from './validator.js';

const SECRET = 's'.repeat(32);
beforeEach(() => { process.env.WEBHOOK_SECRET = SECRET; });

function sign(body: string): string {
  return `sha256=${createHmac('sha256', SECRET).update(body).digest('hex')}`;
}

describe('validateRequest', () => {
  const body = JSON.stringify({ eventType: 'iModels.iModelCreated.v1', iTwinId: 't1' });

  it('accepts a correctly signed body', () => {
    expect(validateRequest(body, sign(body))).toBe(true);
  });

  it('rejects a tampered body', () => {
    expect(validateRequest(body + 'x', sign(body))).toBe(false);
  });

  it('rejects a signature from a different secret', () => {
    const other = `sha256=${createHmac('sha256', 'k'.repeat(32)).update(body).digest('hex')}`;
    expect(validateRequest(body, other)).toBe(false);
  });

  it('rejects a malformed signature header without throwing', () => {
    expect(validateRequest(body, 'not-a-signature')).toBe(false);
  });
});
```

- [ ] **Step 2: RED/特征确认** — Run: `npx vitest run src/validator.test.ts`。全部 PASS = 特征测试成立（现行为正确）；有 FAIL = 发现真 bug，先修 validator 再过。

- [ ] **Step 3: 104 行空 catch 治理** — 阅读该 catch 上下文：若属「预期内的格式异常」改为 `logger.warn('signature parse failed', { reason })` 后返回 false；若掩盖编程错误则 rethrow。以 Step 1 测试保持绿为准。

- [ ] **Step 4: GREEN + Commit** — Run: `npx vitest run src/validator.test.ts`（4/4）→ `git add webhook-agent/src/validator.ts webhook-agent/src/validator.test.ts` → `test(webhook-agent): lock validator behavior; surface swallowed parse errors`

---

### Task 4: forwarder 测试（注入 fetch/定时器）

**Files:**
- Test: `webhook-agent/src/forwarder.test.ts`
- Modify: `webhook-agent/src/forwarder.ts`（仅当测试暴露问题时）

**Interfaces:**
- Consumes: `EventForwarder`（构造参数 `Partial<ForwarderConfig>`）、`config.BACKEND_API_KEY`、`ProcessedEvent`。
- Produces: 重试/放弃语义锁定：`retryAttempts` 内重试、超限标记失败不丢事件对象、成功后从 pending 清除。

- [ ] **Step 1: 写测试**（用 vi.useFakeTimers 推进退避；stubGlobal fetch 模拟 MS：先 2 次 500 后 200；无 key/错 key 请求头断言）

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { EventForwarder } from './forwarder.js';
import type { ProcessedEvent } from './types.js';

const realFetch = global.fetch;
let calls: Array<{ url: string; headers: Record<string, string> }>;

function makeEvent(id: string): ProcessedEvent {
  return {
    eventType: 'iModels.ChangesetPushed.v1', iTwinId: 't1', messageId: id,
    content: {}, id, status: 'completed',
  } as ProcessedEvent;
}

beforeEach(() => {
  process.env.BACKEND_API_KEY = 'b'.repeat(32);
  calls = [];
  vi.useFakeTimers();
});

afterEach(() => { vi.useRealTimers(); vi.unstubAllGlobals(); });

describe('EventForwarder', () => {
  it('sends X-API-Key and forwards on success, clearing pending', async () => {
    vi.stubGlobal('fetch', (async (i: Parameters<typeof fetch>[0], init?: Parameters<typeof fetch>[1]) => {
      calls.push({ url: String(i), headers: (init?.headers ?? {}) as Record<string, string> });
      return new Response('{}', { status: 200 });
    }) as typeof fetch);
    const f = new EventForwarder({ backendUrl: 'http://ms.test:4001', timeout: 1000, retryAttempts: 3, retryDelay: 10 });
    const ok = await f.forwardEvent(makeEvent('e1'));
    expect(ok).toBe(true);
    expect(calls[0].url).toContain('http://ms.test:4001');
    expect((calls[0].headers as any)['X-API-Key'] ?? (calls[0].headers as any)['x-api-key']).toBe('b'.repeat(32));
    expect(f.getPendingCount()).toBe(0);
  });

  it('retries failures and gives up after retryAttempts, keeping the event observable', async () => {
    let n = 0;
    vi.stubGlobal('fetch', (async () => { n++; return new Response('{}', { status: 500 }); }) as typeof fetch);
    const f = new EventForwarder({ backendUrl: 'http://ms.test:4001', timeout: 1000, retryAttempts: 3, retryDelay: 10 });
    const ok = await f.forwardEvent(makeEvent('e2'));
    expect(ok).toBe(false);
    expect(n).toBeGreaterThanOrEqual(2); // at least one retry occurred
    expect(f.getRetryQueueLength() + f.getPendingCount()).toBeGreaterThan(0); // event not silently dropped
  });
});
```

- [ ] **Step 2: RED/特征确认 → 修 → GREEN** — 若现有实现与断言冲突（例如首败即丢），修 forwarder 至测试过；`npx vitest run src/forwarder.test.ts` 2/2。

- [ ] **Step 3: Commit** — `test(webhook-agent): lock forwarder retry/abandon semantics with injected fetch`

---

### Task 5: processor / webhook-server key / baseline SAS 测试与 catch 治理

**Files:**
- Test: `webhook-agent/src/processor.test.ts`、`webhook-agent/src/webhook-server.test.ts`
- Modify: `webhook-agent/src/webhook-server.ts`（提取 `isValidAgentApiKey` 导出 + 116/182/243 三处 catch）、`webhook-agent/src/main.ts`（306 catch）、`webhook-agent/src/baseline-generator.ts`（提取 `buildSasPermissions` 导出）

**Interfaces:**
- Consumes: `EventProcessor`、`IncomingWebhookEvent`（processor.ts 既有导出）、`config.WEBAGENT_API_KEY`。
- Produces: `export function isValidAgentApiKey(headerValue: string | undefined): boolean`（webhook-server.ts）；`export function buildSasPermissions(write: boolean): { read: boolean; write: boolean; delete: boolean; list: boolean }`（baseline-generator.ts，与 Azure SDK 权限对象同形）。processor 分发语义锁定；四处 catch 逐点治理（warn/error 带上下文，不得新增空 catch）。

- [ ] **Step 1: processor 特征测试**

```typescript
import { describe, expect, it, vi } from 'vitest';
import { EventProcessor } from './processor.js';
import type { IncomingWebhookEvent } from './processor.js';

const evt = (type: string): IncomingWebhookEvent => ({
  eventType: type as any, iTwinId: 't1', messageId: 'm1', content: { imodelId: 'im-1' },
});

describe('EventProcessor', () => {
  it('dispatches to handlers registered for the event type and to wildcards', async () => {
    const p = new EventProcessor({});
    const specific = vi.fn(); const wild = vi.fn();
    p.on('iModels.ChangesetPushed.v1', specific);
    p.on('*', wild);
    await p.processEvent(evt('iModels.ChangesetPushed.v1'));
    expect(specific).toHaveBeenCalledTimes(1);
    expect(wild).toHaveBeenCalledTimes(1);
  });

  it('isolates handler failures: one throwing handler does not prevent others', async () => {
    const p = new EventProcessor({});
    const bad = vi.fn(() => { throw new Error('boom'); });
    const good = vi.fn();
    p.on('iModels.ChangesetPushed.v1', bad);
    p.on('iModels.ChangesetPushed.v1', good);
    const result = await p.processEvent(evt('iModels.ChangesetPushed.v1'));
    expect(good).toHaveBeenCalledTimes(1);
    expect(result.status).toBe('completed'); // handler error is logged, not fatal
  });

  it('marks event failed when no handlers exist only if configured; default completes silently', async () => {
    const p = new EventProcessor({});
    const r = await p.processEvent(evt('iModels.iModelDeleted.v1'));
    expect(['completed', 'received']).toContain(r.status);
  });
});
```

（断言以现行为为准——若 processor.on 的注册 API 形状不同，按实际 API 调整测试调用方式，但三个语义断言不变。）

- [ ] **Step 2: RED/特征确认 → GREEN** — `npx vitest run src/processor.test.ts` 3/3。

- [ ] **Step 3: webhook-server key 校验测试**（先提取再测——`webhook-server.ts` 中 `/baseline/retry` 入口的 `req.headers['x-api-key'] !== config.WEBAGENT_API_KEY` 判断提取为导出纯函数 `isValidAgentApiKey`，路由改调用它）：

```typescript
import { describe, expect, it, beforeEach } from 'vitest';
import { isValidAgentApiKey } from './webhook-server.js';

describe('isValidAgentApiKey', () => {
  beforeEach(() => { process.env.WEBAGENT_API_KEY = 'w'.repeat(32); });
  it('accepts the configured key', () => expect(isValidAgentApiKey('w'.repeat(32))).toBe(true));
  it('rejects wrong/missing keys', () => {
    expect(isValidAgentApiKey('wrong')).toBe(false);
    expect(isValidAgentApiKey(undefined)).toBe(false);
  });
});
```

- [ ] **Step 4: baseline SAS 权限构造测试**（`_generateSasToken` 内联的 permissions 对象提取为导出 `buildSasPermissions(write: boolean)`，原调用点改用）：

```typescript
import { describe, expect, it } from 'vitest';
import { buildSasPermissions } from './baseline-generator.js';

describe('buildSasPermissions', () => {
  it('grants read+list for readers and read+write+list for writers', () => {
    expect(buildSasPermissions(false)).toEqual({ read: true, write: false, delete: false, list: true });
    expect(buildSasPermissions(true)).toEqual({ read: true, write: true, delete: false, list: true });
  });
});
```

（若现有权限组合与断言不同，以现行为特征化断言——先测现状，不改语义。）

- [ ] **Step 5: 四处 catch 治理** — webhook-server 116（JSON parse 失败：已有 400 响应，catch 内补 `logger.warn` 带 body 长度）；182 与 243（读上下文：可恢复路径补 `logger.warn` 带路由名，掩盖失败的上 `logger.error`）；main 306（同法，带恢复检查器上下文）。治理后套件回归。

- [ ] **Step 6: Commit** — `test(webhook-agent): lock processor/webhook-key/SAS semantics; surface four swallowed catches`

---

### Task 6: IModelHost 生命周期配对

**Files:**
- Create: `webhook-agent/src/lifecycle.ts`
- Test: `webhook-agent/src/lifecycle.test.ts`
- Modify: `webhook-agent/src/main.ts`（入口接线 + 信号处理）、`webhook-agent/src/baseline-generator.ts`（删 306/443/661 三处 `IModelHost.startup()` 与 897 的局部 shutdown）

**Interfaces:**
- Consumes: Task 1 logger、Task 4 `EventForwarder.shutdown()`。
- Produces: `export async function ensureIModelHostStarted(): Promise<void>`（幂等，进程内唯一 startup）、`export async function shutdownAll(forwarder?: { shutdown(): Promise<void> }): Promise<void>`（IModelHost.shutdown + forwarder flush，幂等）。main.ts 的 SIGTERM/SIGINT handler 调用 `shutdownAll`。

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, expect, it, vi } from 'vitest';

vi.mock('@itwin/core-backend', () => ({
  IModelHost: {
    startup: vi.fn(async () => {}),
    shutdown: vi.fn(async () => {}),
  },
}));

import { IModelHost } from '@itwin/core-backend';
import { ensureIModelHostStarted, shutdownAll } from './lifecycle.js';

describe('lifecycle', () => {
  it('starts IModelHost exactly once across repeated calls', async () => {
    await ensureIModelHostStarted();
    await ensureIModelHostStarted();
    await ensureIModelHostStarted();
    expect(IModelHost.startup).toHaveBeenCalledTimes(1);
  });

  it('shutdown shuts IModelHost down once and flushes the forwarder, and is idempotent', async () => {
    const forwarder = { shutdown: vi.fn(async () => {}) };
    await shutdownAll(forwarder);
    await shutdownAll(forwarder);
    expect(IModelHost.shutdown).toHaveBeenCalledTimes(1);
    expect(forwarder.shutdown).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: RED → 实现 lifecycle.ts**（模块级 `let started = false`/`let shutDown = false` 守卫；`ensureIModelHostStarted` 内 `await IModelHost.startup()`；`shutdownAll` 顺序：forwarder.shutdown() → IModelHost.shutdown()，每步 try/catch → `logger.error`，幂等标志最后置位）

- [ ] **Step 3: GREEN** — `npx vitest run src/lifecycle.test.ts` 2/2

- [ ] **Step 4: 接线** — baseline-generator 三处 `await IModelHost.startup()` 替换为 `await ensureIModelHostStarted()`（import 自 lifecycle）；897 局部 shutdown 删除（全局负责）；main.ts 启动处调 `ensureIModelHostStarted()`，注册 `process.on('SIGTERM'/'SIGINT')` → `await shutdownAll(forwarder)` → `process.exit(0)`（防重入守卫已在 shutdownAll 内）。

- [ ] **Step 5: 回归 + Commit** — `npx vitest run && npx tsc --noEmit` 全绿 → `feat(webhook-agent): single global IModelHost lifecycle with signal-triggered shutdown`

---

### Task 7: 去重状态持久化

**Files:**
- Create: `webhook-agent/src/processedEventsStore.ts`
- Test: `webhook-agent/src/processedEventsStore.test.ts`
- Modify: `webhook-agent/src/main.ts`（`processingIModels` 判重接线到 store；如另有恢复检查器去重 Set 一并接入）、根 `.gitignore`（`data/`）

**Interfaces:**
- Consumes: Task 1 logger、`config`。
- Produces: `export interface ProcessedEventsStore { has(id: string): boolean; add(id: string): void; persist(): void; size(): number }`；`export function createProcessedEventsStore(filePath: string, ttlMs = 120_000): ProcessedEventsStore`。文件格式 `{"events":[{"id":"...","ts":123}]}`。

- [ ] **Step 1: 写失败测试**

```typescript
import { afterEach, describe, expect, it } from 'vitest';
import { mkdtempSync, rmSync, writeFileSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createProcessedEventsStore } from './processedEventsStore.js';

let dir: string;
afterEach(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });

describe('processedEventsStore', () => {
  it('round-trips entries across restarts and persists on add', () => {
    dir = mkdtempSync(join(tmpdir(), 'pes-'));
    const file = join(dir, 'processed-events.json');
    createProcessedEventsStore(file).add('im-1');
    const reloaded = createProcessedEventsStore(file);
    expect(reloaded.has('im-1')).toBe(true);
    expect(reloaded.has('im-2')).toBe(false);
  });

  it('expires entries older than the TTL on load', () => {
    dir = mkdtempSync(join(tmpdir(), 'pes-'));
    const file = join(dir, 'processed-events.json');
    writeFileSync(file, JSON.stringify({ events: [{ id: 'old', ts: Date.now() - 60 * 60 * 1000 }] }));
    const store = createProcessedEventsStore(file, 120_000);
    expect(store.has('old')).toBe(false);
  });

  it('recovers from a corrupted file with an empty store', () => {
    dir = mkdtempSync(join(tmpdir(), 'pes-'));
    const file = join(dir, 'processed-events.json');
    writeFileSync(file, '{not json');
    const store = createProcessedEventsStore(file);
    expect(store.size()).toBe(0);
    store.add('im-3');
    expect(JSON.parse(readFileSync(file, 'utf8')).events).toHaveLength(1);
  });
});
```

- [ ] **Step 2: RED → 实现**（构造时 load+ttlSweep：损坏 → `logger.warn` + 空库；`add` 内存+立即 persist 同步写；has O(1) Map）

- [ ] **Step 3: GREEN** — 3/3 PASS

- [ ] **Step 4: 接线 main.ts** — `processingIModels` 的查重分支改查 store（`data/processed-events.json` 路径相对 cwd 固定 `data/processed-events.json`）；根 `.gitignore` 追加 `data/`。

- [ ] **Step 5: 回归 + Commit** — `npx vitest run && npx tsc --noEmit` → `feat(webhook-agent): persist recovery dedup across restarts (JSON store, TTL 2min)`

---

### Task 8: 收尾验收

**Files:** 无新文件（验证；如有小修照常提交）

- [ ] **Step 1: 全量** — `cd webhook-agent && npx vitest run`（≥ 12+3+4+2+5+2+3 = 31 过）+ `npx tsc --noEmit` 0 错
- [ ] **Step 2: 验收 grep** — `grep -rn "console\." src --include=*.ts | grep -v "\.test\.ts" | grep -v config.ts | grep -v utils/logger.ts` → 空；`grep -rn "catch {}" src --include=*.ts | grep -v test` → 空
- [ ] **Step 3: 重启冒烟（可选，需栈）** — 起 WA → 杀进程 → 复起：恢复检查器不重复触发已处理项（data 文件内容佐证）
- [ ] **Step 4: 汇报**（含每项验收证据）
